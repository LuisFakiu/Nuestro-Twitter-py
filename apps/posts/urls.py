"""URLs de posts. Placeholder — Luis las completa Sem 2."""
from django.urls import path

app_name = 'posts'

urlpatterns = [
    # path('posts/', views.PostListCreateView.as_view(), name='list'),
    # path('feed/', views.FeedView.as_view(), name='feed'),
]
